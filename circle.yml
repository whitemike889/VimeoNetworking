general:
  artifacts:
    - "xcodebuild.log"
    - "build/Logs"
machine:
  xcode:
    version: 8.3.2

dependencies:
    override:
        - bundle install
    cache_directories:
        - "~/.cocoapods"
        - "vendor/bundle"

test:
  override:
    - bundle exec fastlane test
